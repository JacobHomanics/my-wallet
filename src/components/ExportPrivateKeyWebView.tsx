/**
 * Web stub — private key export runs in-page via Privy’s modal, not a WebView.
 */
export function ExportPrivateKeyWebView(_props: {
  uri: string | null;
  onClose: () => void;
}) {
  return null;
}
